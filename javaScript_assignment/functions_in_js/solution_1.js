// Define the arrow function
const square = (num) => num ** 2;

// Example usage: Calculate the square of a given number
const number = 5;
const result = square(number);

// Display the result
console.log(`The square of ${number} is ${result}.`);